import { Command } from '@oclif/core';
export declare abstract class CLICommand extends Command {
    log(message?: string): void;
    time(label: string, format?: string | undefined): {
        end: () => void;
    };
    measure(label: string, format: string, closure: () => Promise<void>): Promise<void>;
}
//# sourceMappingURL=command.d.ts.map